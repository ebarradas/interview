<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Car List</title>
</head>
<body>
    <h1>Car List</h1>
    <ul>
      <% cars.forEach(function(car){ %>
        <li><%= car.id %>, <%= car.model %>, <%= car.make %>, <%= car.year %></li>
      <% }); %>
    </ul>
</body>
</html>